# Base URL of the Aurivest backend API.
# Leave unset (or delete this file) to run against the built-in demo backend
# in src/lib/mockApi.js — the app is fully explorable without a server.
# VITE_API_URL=http://localhost:5000
